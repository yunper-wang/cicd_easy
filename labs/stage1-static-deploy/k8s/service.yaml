# Stage 1 Service —— ClusterIP 暴露应用
# 类型: ClusterIP（集群内访问）
# 如果需要外部访问，配合 Ingress 使用
apiVersion: v1
kind: Service
metadata:
  name: stage1-svc
  labels:
    app: stage1-app
spec:
  type: ClusterIP
  selector:
    app: stage1-app
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
