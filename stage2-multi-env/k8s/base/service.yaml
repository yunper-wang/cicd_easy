# Stage 2 Base Service —— 所有环境共享
apiVersion: v1
kind: Service
metadata:
  name: stage2-svc
  labels:
    app: stage2-app
spec:
  type: ClusterIP
  selector:
    app: stage2-app
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
